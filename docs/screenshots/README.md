# Screenshots

Place screenshots here referenced from the root `README.md` "Screenshots" section:

- `dashboard.png` — the `/dashboard` overview page
- `projects.png` — the `/projects` list page
- `project-detail.png` — a project detail page showing its site map
- `site-draw.png` — the site create/edit page with a polygon drawn on the map
- `site-analytics.png` — a site's analytics page with charts

Capture these at a desktop viewport (e.g. 1440x900) after running the app locally with the
seeded demo data (`docker compose up --build` then `docker compose exec backend python -m scripts.seed`,
log in with `admin@darukaa.earth` / `Demo@12345`).
